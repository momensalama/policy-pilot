services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: policy_pilot
      POSTGRES_USER: policy_pilot
      POSTGRES_PASSWORD: policy_pilot_secret
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
